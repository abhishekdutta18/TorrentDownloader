import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.torrentdownloader.app',
  appName: 'Torrent Downloader',
  webDir: 'dist'
};

export default config;
